enum Action {

}

pub struct ActionContainer {
    //actions: HashMap<
}

impl ActionContainer {
    fn new() -> Self {
        Self
    }
}

impl Default for ActionContainer {
    fn default() -> Self {
        Self::new()
    }
}